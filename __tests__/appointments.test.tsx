// UI tests for Appointments were removed because they rely on full RN rendering. Use
// `appointments.service.test.ts` for deterministic service-level tests instead.

test.skip('appointments UI tests are intentionally skipped in CI', () => {});
